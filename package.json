{
  "name": "svelte-doctor",
  "version": "0.3.6",
  "description": "Diagnose and fix performance, correctness, and architecture issues in your Svelte codebase",
  "keywords": [
    "svelte",
    "svelte5",
    "sveltekit",
    "diagnostics",
    "linter",
    "performance",
    "runes",
    "migration",
    "codemod",
    "cli",
    "static-analysis",
    "code-quality",
    "typescript",
    "security",
    "refactoring",
    "ai",
    "analyzer",
    "vite",
    "javascript"
  ],
  "license": "Apache-2.0",
  "author": "Pimatis",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Pimatis/svelte-doctor.git"
  },
  "homepage": "https://github.com/Pimatis/svelte-doctor",
  "bugs": {
    "url": "https://github.com/Pimatis/svelte-doctor/issues"
  },
  "type": "module",
  "packageManager": "bun@1.3.10",
  "bin": {
    "svelte-doctor": "./dist/cli.mjs"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "default": "./dist/index.mjs"
    }
  },
  "publishConfig": {
    "provenance": true
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "tsdown --watch",
    "build": "rm -rf dist && NODE_ENV=production tsdown && rm -f dist/*.map && node -e \"const fs=require('node:fs'); for (const file of fs.readdirSync('dist')) { if (!file.endsWith('.mjs')) continue; const path='dist/' + file; const source=fs.readFileSync(path,'utf8').replace(/^\\/\\/\\# sourceMappingURL=.*$/gm,'').replace(/\\n{3,}/g,'\\n\\n'); fs.writeFileSync(path, source, 'utf8'); }\"",
    "check": "tsc --noEmit",
    "prepack": "bun run build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "commander": "^15.0.0",
    "knip": "^6.32.2",
    "ora": "^9.4.1",
    "picocolors": "^1.1.1",
    "svelte": "^5.56.9",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.2.0",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.6",
    "tsdown": "^0.22.14",
    "typescript-eslint": "^8.67.0"
  }
}
